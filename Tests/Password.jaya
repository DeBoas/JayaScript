i en idiota
npme User authentication
senha user=User: 
senha password=Password: 
si sim user==root(
  aparecer Ok
  si sim password==1234(
    aparecer Welcome
  ) else (
    hac exit
  )
) si no (
  hac exit
)
